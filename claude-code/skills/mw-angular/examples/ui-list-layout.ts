import { Component } from '@angular/core';

/**
 * Template: layout and projection slots, no data and no logic.
 * An empty class here is the point - it is a design system guardrail, not a component.
 */
@Component({
  selector: 'app-ui-list-layout',
  templateUrl: './ui-list-layout.html',
  styleUrl: './ui-list-layout.css',
})
export class UiListLayout {}
